# Project Metadata
# Generated by Go-Arch CLI 🚀

project_name: portfolio-go
module_name: github.com/SalvucciFacundo/portfolio-go
architecture: Hexagonal
# Template pack — set by go-arch when scaffolding from a pack.
# template: <pack-name>  # e.g. go-arch template install <mod>@<ver>
db_driver: PostgreSQL
use_docker: true
use_observability: false
observability_backend: 
use_grpc: false
use_templ_htmx: true
go_arch_version: dev
generated_at: 2026-08-12 19:31:40

# Hooks — optional lifecycle extensibility.
# Uncomment and customise to run shell commands around scaffold/generate.
# ⚠  Executable surface: hooks run arbitrary commands with your shell.
#    Treat .go-arch.yaml like npm scripts or a Makefile — review before
#    checking it into source control. See docs/hooks.md for details.
#
# Supported types: pre-new, post-new, pre-generate, post-generate.
# Each entry can be a string (shell shorthand) or an object.
#
# hooks:
#   pre-new:
#     - echo "About to scaffold portfolio-go…"
#   post-new:
#     - command: go
#       args: [mod, tidy]
#       timeout: 60s
#     - command: git
#       args: [init]
#       ignore_failure: true
#   pre-generate: []
#   post-generate:
#     - gofmt -w .
#     - command: go
#       args: [vet, ./...]
#       silent: true
#       ignore_failure: true
